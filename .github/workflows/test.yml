name: Branch name filtering

on:
  pull_request:
    branches:
      - '!thisshouldnottrigger'

jobs:
  job1:
    name: Job 1
    runs-on: ubuntu-latest

    steps:
      - name: Run command
        run: |
          echo oi mundo
